{
  "buildCommand": "pip install -r requirements.txt || true",
  "outputDirectory": ".",
  "framework": "python",
  "functions": {
    "api/stats.py": {
      "runtime": "python3.9",
      "maxDuration": 30
    }
  }
}
